# ==============================================================================
# AWS Credentials Configuration
# ==============================================================================
# Option 1: Use explicit AWS credentials (copy this file to .env and configure)
# Option 2: Leave commented to use AWS CLI credentials or instance profiles

# AWS Access Key ID
AWS_ACCESS_KEY_ID=your-access-key-id

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# AWS Session Token (optional - only for temporary credentials from STS/IAM roles)
# AWS_SESSION_TOKEN=your-session-token

# AWS Region (optional - defaults to us-east-1)
# AWS_DEFAULT_REGION=us-east-1

# ==============================================================================
# Iceberg Catalog Configuration
# ==============================================================================
# Choose catalog type: "s3tables" or "glue"
CATALOG_TYPE=s3tables

# ==============================================================================
# Option 1: AWS S3 Tables Configuration
# ==============================================================================
# Use this when CATALOG_TYPE=s3tables
# Specify the ARN of your S3 Tables bucket
S3_TABLE_BUCKET_ARN=arn:aws:s3tables:us-east-1:123456789012:bucket/your-table-bucket

# ==============================================================================
# Option 2: AWS Glue Data Catalog Configuration
# ==============================================================================
# Use this when CATALOG_TYPE=glue
# Specify the S3 warehouse path
S3_WAREHOUSE=s3a://your-bucket-name/warehouse

# ==============================================================================
# Additional S3 Configuration (Optional)
# ==============================================================================
# S3 Endpoint (optional - for custom endpoints or regions)
# AWS_S3_ENDPOINT=s3.amazonaws.com

# Enable S3 path style access (optional - for custom S3-compatible storage)
# AWS_S3_PATH_STYLE_ACCESS=false

# ==============================================================================
# Application Configuration (Optional)
# ==============================================================================
# Spark application name override
# SPARK_APP_NAME=My Spark Iceberg Application

# Log level (optional - DEBUG, INFO, WARN, ERROR)
# LOG_LEVEL=INFO
